export enum ComparisonName {
    less = "小于", lessOrEquals = "小于等于",
    greater = "大于", greaterOrEquals = "大于或等于",
    equals = "等于", notEquals = '不等于',
    between = "区间内", notBetween = '区间外',
    like = '类似', notLike = '不类似',
    contains = '包含', notContains = '不包含'
}